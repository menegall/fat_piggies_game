package com.fatpiggies.game.model.ecs.systems;

/**
 * Recalculates the currentMaxVelocity, currentMaxAcceleration,
 * and currentMass of an entity by summing its base values with all active modifiers found
 * in attached entities (Buffs).
 * Attention: It has to be done only once, not all frames!
 */
public class StatSystem {

}
